<%= render partial:"blog/public/header", locals:{ str: "bar" } %>

<%= render 'blog/public/flash_notice' %>

<div class="row">
  <div class="col-md-8 main">
    <div class="main-hd">
      <h3>最近的动向</h3>
    </div>
    <div class="main-bd">
      <ul class="events">

        <% @events.each do |theevent| %>
          <div class="event">

            <li id="event-<%= theevent.id %>">
              <%= link_to theevent.title, [:blog, theevent] , id:"title"%>
              <span class="timestamps">
                Edited
                <%= format_datetime(theevent.updated_at) %>
              </span>
              <p class="body"><%= sanitize markdown(theevent.content).to_s[0..450] %></p>
              <span class="posts">
                <ul class="posts">
                  <%= render theevent.posts[0..2] %>
                </ul>
              </span>

            </li>
          </div>
        <% end %>
        <%= will_paginate @events %>
      </ul>
    </div>

  </div>
</div>
